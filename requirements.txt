# Python dependencies for the nav-stack module (builtin SLAM + nav).
viam-sdk>=0.30.0
numpy>=1.24
PyYAML>=6.0
# Pillow renders the nav-camera occupancy/plan overlay to PNG.
Pillow>=10.0
pyserial>=3.5
# Optional TypeSafe/Jev overlay (builtin.nav_policy = shadow|jev).
typesafe-sdk>=0.7.0
